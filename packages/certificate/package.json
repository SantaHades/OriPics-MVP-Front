{
  "name": "@oripics/certificate",
  "version": "0.0.1",
  "description": "증명서 PDF 렌더 — Next 서버 번들 밖(serverExternalPackages)에서 실행되어 react-pdf와 React 사본이 일치한다. 번들되면 Next 내장 React 19 별칭과 어긋나 renderToBuffer가 React error #31로 실패(2026-08-28 실측). 소스 수정 시 `npm run build` 후 산출물(index.js·render.js)을 함께 커밋할 것.",
  "main": "index.js",
  "types": "index.d.ts",
  "scripts": {
    "build": "esbuild src/index.ts src/render.tsx --outdir=. --format=cjs --platform=node --jsx=transform"
  }
}
