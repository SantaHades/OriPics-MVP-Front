{
  "name": "@oripics/certificate",
  "version": "0.0.1",
  "description": "증명서 PDF 렌더 — Next 서버 번들 밖(serverExternalPackages)에서 실행. react-pdf 전체를 이 산출물에 번들(자기완결, react만 외부)해 서버리스 트레이싱 누락·ESM require 문제를 차단. 번들 없이는 Next 내장 React 별칭과 어긋나 error #31(2026-08-28 실측). 소스 수정 시 npm run build 후 index.js 커밋.",
  "main": "index.js",
  "types": "index.d.ts",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --outfile=index.js --format=cjs --platform=node --jsx=transform --external:react"
  }
}
